from typing import Iterable


def count_direction_changes(numbers: Iterable[int]) -> int:
    """
    Count how many times the direction changes between up and down.

    Rules:
    - Ignore non-integers.
    - Equal values do not change direction.
    """
    pass
